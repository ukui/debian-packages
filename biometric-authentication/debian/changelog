biometric-authentication (0.9.55) cosmic; urgency=medium

  * The driver configuration file rename to biometric-drivers.conf
  * Add the determination of whether the key value exists for the
    biometric-config-tool
  * Encrypt the characteristic value of community multidevice driver

 -- jianglinxuan (droiing) <jianglinxuan@kylinos.cn>  Thu, 06 Sep 2018 15:08:33 +0800

biometric-authentication (0.9.54kord) cosmic; urgency=medium

  * Modify kylin to ukui

 -- jianglinxuan (droiing) <jianglinxuan@kylinos.cn>  Mon, 03 Sep 2018 17:33:09 +0800

biometric-authentication (0.9.53kord) xenial; urgency=medium

  * Add community multidevice driver and discover tools
    - Add community multidevice driver
    - Add community multidevice discover tools

 -- jianglinxuan (droiing) <jianglinxuan@kylinos.cn>  Fri, 31 Aug 2018 17:29:47 +0800

biometric-authentication (0.9.52kord) xenial; urgency=medium

  * Modify the database default permissions to improve security
  * BugFix: Missing "org.freedesktop.policykit.pkexec.biometric-authentication
            .policy" file
  * Add manpages for the biometric-utils package
  * Use libtool to compile libbiometric
  * Frameworks, a210 driver, and R301 driver add support for all architectures
  * Add the debug package for the drivers(ud650, a210, r301)
  * Add Copyright
  * Add external interface instructions and adjust code block order
  * Solve the lintian related problems:
    - maintainer-script-lacks-debhelper-token
    - maintainer-script-calls-systemctl

 -- jianglinxuan (droiing) <jianglinxuan@kylinos.cn>  Fri, 31 Aug 2018 15:29:16 +0800

biometric-authentication (0.9.51kord) xenial; urgency=medium

  * Release:  0.9.52kord
    - Modify the database default permissions to improve security
    - BugFix: Missing "org.freedesktop.policykit.pkexec.biometric-authentication
             .policy" file
    - Add manpages for the biometric-utils package
    - Use libtool to compile libbiometric
    - Frameworks, a210 driver, and R301 driver add support for all architectures
    - Add the debug package for the drivers(ud650, a210, r301)
    - Add Copyright
    - Add external interface instructions and adjust code block order
    - Solve the lintian related problems:
      * maintainer-script-lacks-debhelper-token
      * maintainer-script-calls-systemctl

  * Release:  0.9.51kord1
    - BugFix: The service's systemd logs are incomplete
    - BugFix: Policykit description and message internationalization is invalid
    - Features: Add the policykit description for the biometric-config-tool

  * Release:  0.9.50kord
    - Modify database format and add database upgrade function
      * Add the UUID field in the EIGEN_INFO table
      * Add database version detection function
      * Add database format upgrade function
    - Improve the robustness of data migration
      * Use transaction mode to migrate data so that it can be rollback when it fails
      * Back up the original EIGEN_INFO table to EIGEN_INFO_VERSION_0_0_0
    - Add new constraints when getting and storing features: UUID

  * Release:  0.9.49kord
    - New function: Service running environment detection
      * Detect whether the service has permissions;
      * Detect whether the service is running repeatedly;

  * Release:  0.9.48kord
    - Add timestamps for three types of messages
      * Interface of change: UpdateStatus, GetDevMesg, GetOpsMesg, GetNotifyMesg
        - Timestamp type: struct timeval (sys/time.h)
        - The APP_API is upgraded to 0.11.0

  * Release:  0.9.47kord
    - Bug Fix: Fix the clean operation's ops_result value wrong
      * OPS_CLEAN_xxx starts at OPS_TYPE_CLEAN * 100, not OPS_TYPE_CLEAN
    - Enhancement: Add type definitions
      * Add a definition for the sound print: BioT_VoicePrint
      * Add two bus type definitions: BusT_Any and BusT_Other

  * Release:  0.9.46kord
    - Bug Fix: The driver ID that the driver dynamically
      gets is no longer 100 forever

  * Release:  0.9.45kord
    - Enhancement: Improve the get_feature_info operation speed
    - New features: Add dbus signal for hot pluggable events
    - Bug Fix: Dev_num is changed to 0 when the finger vein
      device does not exist
    - Upgrade DRV_API and APP_API

  * Release:  0.9.44kord
    - Fix: hot plug and unplug USB device caused segmentation fault
    - When the device is disabled, hot plug and unplug no longer
      discover device
    - Update the output of the driver list and device list

  * Release:  0.9.43kord
    - Add a new dbus return value: RetOpsNoMatch
      * RetOpsNoMatch indicates that there are no errors in the
      verify and identity operations and the results do not match
      * Modify the return value of verify and identity
        - The process is normal and the result matches,
          return -RetOpsSuccess(0);
        - The process is normal and the results do not match,
          return -RetOpsNoMatch(-1);
    - Update po file
    - Update the APP_API version number
    - Add the debug package

  * Release:  0.9.42kord
    - Temporarily remove the soname
    - New features: add the ability to dynamically get the driver ID
    - Add BioOpsType to enumerate each type of operation
    - New features: Add driver operation check function for the framework
    - Enhancement: The search method returns all matching features
    - The framework adds a judgment on whether the device is enabled
    - Normalize the return value of the dbus
    - Adjust the DRV_API and APP_API version Numbers for the upcoming official version

  * Release:  0.9.41kord1k
    - New features: The framework adds support for the ID table (USB and PCIe
      device)

  * Release:  0.9.40kord
    - New features: Add compatibility checking mechanism
      * When the driver is loading, the framework checks the compatibility of
        driver's DRV_API version and framework's DRV_API version
      * Add a new DBus interface to check the compatibility of
        application's APP_API version and framework's APP_API version

  * Release:  0.9.39kord
    - New function: Add device count function
      * Biometric framework add device count function
      * R301, UD650 and A210 driver add device count function
    - Bug fix: when a driver fails to load, subsequent drivers cannot be loaded

  * Release:  0.9.38kord
    - New features:
      * Add device node detection mechanism for serial device
      * Add device node detection tool for r301 device

  * Release:  0.9.37kord
    - New features:
      * Add an interface "GetDrvList" to get the driver list
      * Add an interface "bio_common_detect_usb_device" to detect usb device
      * Add the rename feature interface
    - Standardize Makefile.am
      * Fix biometric-authenticationd.h and biometric-authenticationd.c
        compilation dependencies
      * Removes automatically generated interface files
      * Standardize driver link process
      * Standardize extra driver clean process

  * Release:  0.9.36kord1
    - Modify runtime depends:
      * remove debhelper (>= 9)

  * Release:  0.9.36kord
    - Fix the set-key instruction for the biometric-config-tool in the absence
      of driver name
    - Fix Repair the Chinese translation text of biometric-config-tool

  * Release:  0.9.35kord
    - Clean interface bug fix and enhancements
      * Bug fix: The device driver is not specified when the feature is deleted
      * Enhancements: System level cleaning of the specified range of biometric
        features (all drivers, for kylin-control-center)

  * Release:  0.9.34kord
    - The function interface is placed by type,
    - Enhanced code readability and logic:
      * ops_status series function is changed to ops_result
      * notify_status series function is changed to notify_mid

  * Release:  0.9.33kord1
    - Modify the postinst script, all the serial port device drivers are not
      enabled by default
      * Affected driver: R301 fingerprint driver, A210 iris driver

  * Release:  0.9.33kord
    - Fix bug: General users enroll/delete their own biometric feature need
      administrator privileges
      * General users now use their own permissions
      * Modify PolicyKit prompt information

  * Release:  0.9.32kord
    - A210 drivers add internationalization support

  * Release:  0.9.31kord
    - biometric-config-tool add internationalization support

  * Release:  0.9.30kord
    - Fixed UD650 driver: when the operation is interrupted by user, the
      status prompt is abnormal
    - Add a return value of dbus for PolicyKit cancel

  * Release:  0.9.29kord
    - Revise the document field to adapt qdbusxml2cpp

  * Release:  0.9.28kord
    - PolicyKit add internationalization support
    - Fixed: libbiometric package missing translation file

  * Release:  0.9.27kord
    - R301 and UD650 drivers add internationalization support

  * Release:  0.9.26kord
    - Update A210 driver, improve enroll, verify, identify and search function

  * Release:  0.9.25kord
    - The biometric framework adds internationalization support

  * Release:  0.9.24kord
    - New functionality: when device status changes, emit signal

  * Release:  0.9.23kord1
    - When /etc/biometric-auth/biometric-auth.conf is not exist, create it.

  * Release:  0.9.23kord
    - Add utils: biometric-config-tool

  * Release:  0.9.22kord
    - Add iris driver: a210

  * Release:  0.9.21kord2
    - Add "automake" and "libpolkit-gobject-1-dev" to "Build-Depends".

  * Release:  0.9.21
    - Add polkit authentication for Enroll and Clean DBus.

  * Release:  0.9.20
    - biometric-authentication.service autostart

  * Release:  0.9.19
    - Fix: biometric-authentication.service for systemd missing

  * Release:  0.9.18kord
    - Remove unsupport architecture: i386, armhf;
    - Add pkg-config support and static libraries;

 -- jianglinxuan (droiing) <jianglinxuan@kylinos.cn>  Mon, 20 Aug 2018 15:28:39 +0800

biometric-authentication (0.9.17kord) xenial; urgency=medium

  * Initial release.

 -- jianglinxuan (droiing) <jianglinxuan@kylinos.cn>  Mon, 13 Mar 2017 15:30:13 +0800
